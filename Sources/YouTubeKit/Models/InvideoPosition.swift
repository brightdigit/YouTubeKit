//
// InvideoPosition.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
import AnyCodable

/** Describes the spatial position of a visual widget inside a video. It is a union of various position types, out of which only will be set one. */
public struct InvideoPosition: Codable, Hashable {

    public enum CornerPosition: String, Codable, CaseIterable {
        case topleft = "topLeft"
        case topright = "topRight"
        case bottomleft = "bottomLeft"
        case bottomright = "bottomRight"
    }
    public enum ModelType: String, Codable, CaseIterable {
        case corner = "corner"
    }
    /** Describes in which corner of the video the visual widget will appear. */
    public var cornerPosition: CornerPosition?
    /** Defines the position type. */
    public var type: ModelType?

    public init(cornerPosition: CornerPosition? = nil, type: ModelType? = nil) {
        self.cornerPosition = cornerPosition
        self.type = type
    }
    public enum CodingKeys: String, CodingKey, CaseIterable {
        case cornerPosition
        case type
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(cornerPosition, forKey: .cornerPosition)
        try container.encodeIfPresent(type, forKey: .type)
    }



}
